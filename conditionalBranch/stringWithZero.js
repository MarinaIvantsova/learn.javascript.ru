
// Выведется ли alert?

if ("0") {
  alert( 'Привет' );
}

//Выведется, т.е "0" - это не пустая строка, а значит true
//Любая строка, кроме пустой (а строка "0" – не пустая), в логическом контексте становится true.